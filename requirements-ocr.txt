# 与主环境同一 conda 安装（CPU Paddle，勿装 paddlepaddle-gpu）
#
# 正确顺序:
#   pip install -r requirements-ocr.txt
#   pip install -r requirements.txt
# 或 Windows:
#   pip install -r requirements-ocr.txt
#   .\scripts\repair_env_after_ocr.ps1
#
# pip 若提示「rtmlib requires onnxruntime」：属装 OCR 时挤掉了 ORT，执行上一行 repair 即可。
# PaddleOCR 2.7 + imgaug 需要 NumPy 1.x；repair 脚本会 pin numpy<2。
#
paddlepaddle==2.6.2
paddleocr==2.7.3
numpy>=1.24,<2
