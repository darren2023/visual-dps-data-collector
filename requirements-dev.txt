# 开发/脚本测试（在对应平台 GPU 或 CPU 依赖安装完成后可选安装）
#
# Linux GPU:   pip install -r requirements-linux-gpu.txt && pip install -r requirements-dev.txt
# Windows GPU: pip install -r requirements-windows-gpu.txt && pip install -r requirements-dev.txt

-r requirements-base.txt
httpx>=0.27.0
