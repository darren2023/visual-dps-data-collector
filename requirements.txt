opencv-python-headless>=4.8.0
numpy>=1.24.0,<2
# 默认 GPU 推理（与 visual-dps inference-lite-gpu 一致）
# 勿与 onnxruntime（CPU 版）同时安装，否则 import 到 CPU 包、无 CUDAExecutionProvider
onnxruntime-gpu>=1.20.0
# ORT CUDA 需要 cuDNN 9 DLL（Windows 常用 pip 安装，国内镜像较快）
nvidia-cudnn-cu12
rtmlib>=0.0.13
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
openpyxl>=3.1.0
pyarrow>=14.0.0
