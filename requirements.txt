# visual-dps-datacollect 依赖索引
#
# 请按平台选择 GPU 依赖文件（勿直接对本文件 pip install）：
#   Linux:   pip install -r requirements-linux-gpu.txt
#   Windows: pip install -r requirements-windows-gpu.txt
#   CPU:     pip install -r requirements-cpu.txt
#
# 安装 rtmlib（两平台相同，必须 --no-deps）：
#   pip install "rtmlib>=0.0.13" --no-deps
#
# 一键部署（推荐）：
#   Linux:   bash scripts/setup_linux.sh
#   Windows: powershell -ExecutionPolicy Bypass -File scripts/setup_windows.ps1
#
# 开发/测试见 requirements-dev.txt
